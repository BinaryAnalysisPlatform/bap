name: builds-bap-on-many-os
on:
  - push

jobs:
   build:
     runs-on: ubuntu-latest
     steps:
     - uses: actions/checkout@master

     - name: Build BAP on Archlinux
       uses: docker/build-push-action@v1
       with:
         push: false
         dockerfile: docker/archlinux/Dockerfile